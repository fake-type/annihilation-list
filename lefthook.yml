pre-push:
  commands:
    packages-audit:
      tags: frontend security
      run: pnpm audit

pre-commit:
  commands:
    roma-tools:
      tags: static tests and formatting
      glob: "*.{ts,tsx,json}"
      run: pnpm lint
